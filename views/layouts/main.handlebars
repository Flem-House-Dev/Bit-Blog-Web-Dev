<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Super Blog | Welcome</title>

    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
  </head>

  <body>
    <div class="container-fluid">

      <!-- Start Top Bar -->
      <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">

          {{! user name display for small screens }}
          <span class="d-lg-none">
            {{#if user}}
              <p><strong>{{user.username}}</strong></p>
            {{/if}}
          </span>

          {{! hamburger icon for small screen nav menu }}
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          {{! nav menu }}
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
              </li>

              {{#if loggedIn}}
                <li class="nav-item">
                  <a class="nav-link" id="dashboardBtn" href="/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" id="newBlogBtn" href="/blog-form">New Blog</a>
                </li>
                <li class="nav-item">
                  <button class="nav-link" id="logOutBtn">Log Out</button>
                </li>
                <li class="nav-item">
                  <button class="nav-link" id="deleteUserBtn" href="/">Delete
                    Account</button>
                </li>
              {{else}}
                <li class="nav-item">
                  <a class="nav-link" id="logInBtn" href="/login">Log In</a>
                </li>
              {{/if}}
            </ul>
          </div>

          {{! user name display for larger screens }}
          <span class="d-none d-lg-inline">
            {{#if user}}
              <p><strong>{{user.username}}</strong></p>
            {{/if}}
          </span>

        </div>
      </nav>
      <!-- End Top Bar -->

      {{! page banner }}
      <div class="bg-info p-3 mb-3">
        <div class="row column text-center">
          <h1>Bit Blog</h1>
          <h2 class="subheader">Web Development Blog</h2>
        </div>
      </div>

      {{! page content container }}
      <div class="container">
        <div class="row medium-8 large-7 columns">
          <div class="content">
            {{{body}}}
          </div>
        </div>
      </div>

    </div>

    {{! sripts }}
    {{#if loggedIn}}
      <script src="/js/logout.js"></script>
      <script src="/js/delete-user.js"></script>
    {{/if}}

    <script src="https://code.jquery.com/jquery-2.1.4.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    ></script>

  </body>

</html>